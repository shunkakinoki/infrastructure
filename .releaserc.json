{
  "branches": ["master"],
  "plugins": [
    "@semantic-release/commit-analyzer",
    "@semantic-release/release-notes-generator",
    "@semantic-release/changelog",
    [
      "@semantic-release/exec",
      {
        "prepareCmd": "git checkout -b auto-semantic-release && git add -A && git commit -m 'auto-commit' && git push --set-upstream origin auto-semantic-release && yarn run gh pr --submit"
      }
    ],
    [
      "@semantic-release/npm",
      {
        "npmPublish": false,
        "tarballDir": "dist"
      }
    ],
    "@semantic-release/github"
  ]
}
